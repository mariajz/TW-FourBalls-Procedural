plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}


dependencies {
    testImplementation group: 'junit', name: 'junit', version: '5.7'
    implementation group: 'org.processing',name:'core',version: '3.3.7'
}

test {
    useJUnitPlatform()
}